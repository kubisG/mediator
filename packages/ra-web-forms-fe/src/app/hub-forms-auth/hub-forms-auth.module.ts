import { NgModule } from "@angular/core";
import { HubFormsAuthService } from "./hub-forms-auth.service";

@NgModule({
    providers: [
        HubFormsAuthService,
    ]
})
export class HubFormsAuthModule {

}
