{
  "name": "root",
  "private": true,
  "scripts": {
    "bundle": "lerna run --no-bail build",
    "trader": "npm run ra-bundle && lerna run --scope ra-trader-be build:prod"
  },
  "devDependencies": {
    "lerna": "^3.13.3"
  }
}
